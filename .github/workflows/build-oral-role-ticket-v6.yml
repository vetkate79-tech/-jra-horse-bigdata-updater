name: Build oral role ticket v6
on:
  workflow_dispatch:
  push:
    paths:
      - 'src/build_oral_role_ticket_v6.py'
      - '.github/workflows/build-oral-role-ticket-v6.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python src/build_oral_role_ticket_v6.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/data/oral-golden-fast-v6.json
          git diff --cached --quiet && exit 0
          git commit -m 'Build recent-course oral tickets v6'
          git pull --rebase origin main
          git push origin HEAD:main

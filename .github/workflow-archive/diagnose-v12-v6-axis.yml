name: Diagnose V12 V6 axis
on:
  push:
    paths:
      - 'src/diagnose_v12_v6_axis_disagreement.py'
      - '.github/workflows/diagnose-v12-v6-axis.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python src/diagnose_v12_v6_axis_disagreement.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add status/oral-v12-v6-axis-disagreement.json
          git diff --cached --quiet && exit 0
          git commit -m 'Store V12 V6 disagreement diagnostic'
          git pull --rebase origin main
          git push origin HEAD:main
